(example)
(run ((cons 'du 'a)))
(run ((car (cons 'du 'a))))
(run ((eq? 'du 'a)))
(run ((eq? 'du 'du)))
(run ((if (eq? 'du 'a) 'wat? 'ok)))
(run ((if (eq? 'du 'du) 'ok 'wat?)))
(run ((def second (lambda (xs) (car (cdr xs))))
      (def third (lambda (xs) (car (cdr (cdr xs)))))
      (def test (lambda (xs) (cons 'second: (cons (second xs) (cons 'third: (cons (third xs) ()))))))
      (test '(one two three four))))
